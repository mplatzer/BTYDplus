language: r
cache: packages
sudo: required

apt_packages:
  - libgsl0-dev

r_binary_packages:
  - mvtnorm
  - bayesm
  - coda
  - Rcpp
  - gsl
  - lintr

r_packages:
  - BTYD
  - data.table

after_success:
  - Rscript -e 'covr::codecov()'

#warnings_are_errors: true
